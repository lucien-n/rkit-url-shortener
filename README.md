# .env format

```css
PUBLIC_ORIGIN=[string]<short urls origin>
PUBLIC_URL_EXPIRATION_TRESHOLD_DAYS=[number]
CRON_SECRET=[string]
DATABASE_URL=[string]
RATELIMITING_ENABLED=[boolean]
RATELIMITING_DEFAULT_TOKENS=[number]
REDIS_EXPIRATION_MOST_VIEWED_URLS=[number]
REDIS_EXPIRATION_SINGLE_URL=[number]
UPSTASH_REDIS_REST_TOKEN=[number]
UPSTASH_REDIS_REST_URL=[string]
```
